[
{poirot, [
  {modules, [
    {zmq_receiver, [
      {bind, "tcp://*:<%= node['poirot']['receiver']['port'] %>"}
    ]},
    {indexer, [
      {elasticsearch_url, "http://<%= node['poirot']['elasticsearch']['host'] %>/"},
      {prefix, <<"<%= node['poirot']['receiver']['index_prefix'] %>">>}
    ]}
  ]}
]}
].

